<ul>
  {{#each burgers}}
    <li>
      <p>{{this.id}} {{this.burger_name}}</p>
      <button data-id="{{this.id}}" class="devourBurger">Devour</button>
    </li>
  {{/each}}
</ul>

{{!-- Potential button added when burger is devoured. Include in partials? --}}
{{!-- <li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#unless devoured}}Add Burger Again{{else}}Devour{{/if}}
	</button>
</li> --}}