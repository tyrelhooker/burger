{{!-- Displays name and devour state button to page --}}
<li>
	{{burger_name}}
	<div class="progress" style="width:100px">
		<div id="progressbar{{id}}" class="progress-bar progress-bar-striped active" style="width: 2%"></div>
	</div>
	<br>
	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
		{{#if devour}}Devour{{else}}Order Burger Again{{/if}}
	</button>
	<br><br>
</li>